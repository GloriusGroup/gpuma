[pytest]
testpaths = tests
python_files = test_*.py
addopts = -v -ra -m "not slow"
markers =
    slow: marks tests as slow (deselect with '-m "not slow"')
    real_model: marks tests that load real models (bypasses mock fixtures)
